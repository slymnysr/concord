# Dev: Prometheus'un host'ta çalışan API'ye ulaşması için WSL IP'si.
# Oluştur:  echo "HOST_IP=$(hostname -I | awk '{print $1}')" > infra/docker/.env
# NOT: WSL IP'si reboot'ta değişir → yeniden üret. Prod'da gerekmez (API container).
HOST_IP=172.30.36.193
